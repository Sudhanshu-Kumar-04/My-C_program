#include <stdio.h>

int main(){
    int a = 5;
    int b = 2;
    int c = a + b;
    printf("The value of a is %d and value of b is %d and c is %d\n", a, b, c);
    //Modules operator is used to get the remainder 
    printf("The remainder is when a is divided by b is: %d\n", a%b);
    return 0;
}
